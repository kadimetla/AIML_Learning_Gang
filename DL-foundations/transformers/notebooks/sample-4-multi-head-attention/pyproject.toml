[project]
name = "sample-4-multi-head-attention"
version = "0.1.0"
description = "Splitting attention into multiple heads"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "matplotlib>=3.11.0",
    "nbformat>=5.10.4",
    "numpy>=2.5.1",
    "torch>=2.13.0",
]
