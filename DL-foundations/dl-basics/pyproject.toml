[project]
name = "dl-basics"
version = "0.1.0"
description = "Basic building blocks of neural networks, from scratch in numpy, one notebook per chapter"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyterlab>=4.6.3",
    "matplotlib>=3.11.1",
    "nbconvert>=7.17.1",
    "nbformat>=5.11.1",
    "numpy>=2.5.2",
]
