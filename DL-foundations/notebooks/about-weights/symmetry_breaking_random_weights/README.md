# from your bootcamp repo root
#uv init --no-workspace   # only if you don't already have a pyproject.toml
uv init symmetry_breaking_random_weights
uv add jupyterlab numpy pandas matplotlib
uv run jupyter lab
